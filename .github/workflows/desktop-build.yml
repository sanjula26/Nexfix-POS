name: Windows Desktop Build

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'electron/**'
      - 'src/**'
      - 'public/**'
      - 'package.json'
      - 'package-lock.json'
      - 'vite.config.*'
      - '.github/workflows/desktop-build.yml'

permissions:
  contents: read

jobs:
  windows-desktop:
    runs-on: windows-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Verify dependency manifest
        shell: pwsh
        run: |
          git diff --exit-code -- package.json package-lock.json

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Build web app
        run: npm run build

      - name: Build Windows installer and portable app
        run: npm run desktop:build

      - name: Verify Windows artifacts
        shell: pwsh
        run: |
          $files = Get-ChildItem -Path release -File
          if ($files.Count -lt 2) { throw "Expected installer and portable artifacts." }
          if (-not ($files.Name -match '\.exe$').Count) { throw "No Windows .exe artifact was produced." }

      - name: Upload Windows artifacts
        uses: actions/upload-artifact@v4
        with:
          name: nexfix-pos-windows
          path: release/*
          if-no-files-found: error
          retention-days: 14
