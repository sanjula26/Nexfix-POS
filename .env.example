# Nexfix POS — environment variables
# Copy to .env.local for local overrides (optional today — app is client-side only)

# App display name (future use)
VITE_APP_NAME=Nexfix POS

# Future cloud sync / backup targets (not required for offline mode)
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
# VITE_BACKUP_ENDPOINT=
